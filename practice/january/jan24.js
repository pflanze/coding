// CONDITIONAL STATEMENTS

// comparison operator
// negation condition

var s = 9;
vars2 = "10";
// "! " is negation , the output of the  inner brackets is false
if (!(s >= s2) || s1 === s2) {
  console.log("if part");
} else {
  console.log("elese part");
}

// ternary operator is a combination of two operators; ? and : 
// a question (? ) mark separates the condition and the colon (: ) separates the result ;
console.log(1 > 2 ? true : false); //

// IF-else
/**
 * Condition expression evaluates to a boolean value 
 * If the condition expression evaluates to true, than the Block is executed
 * If condition expression evaluates to false, then the block is skipped
 * 
 */

 else = prompt("30");
 age = parseInt(age);
 if (age > 60) {
     document.write()
 }
 else if (age > 30) {
     document.write();
 }
 else {
     document.write();
 }
 // SWITCH  - conditional statemnts 
 /**
  * 
  * test expression 
  * Evaluated to an integer, floating point number, string or boolean value 
  * case statements : 
  * - contain the different values a test expression evaluates to 
  * - Permited case value 
  * - integer, floating-point number, string or boolean values 
  * A group of statements are executed (followed by break statement ) 
  * - break statement - it breaks the execution of group of statements 
  * 
  * - default statement 
  * mached when test expression does not match the listed case statements 
  * 
  */