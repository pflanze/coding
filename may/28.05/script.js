exports.sum = (x, y) => x + y;
exports.sub = (x, y) => x - y;

exports.multi = (x, y) => x * y;
exports.prepareString = (str) => {
  const lower = str[0].toLowerCase();
  console.log(lower);
  toUpperCase() = 

} 
